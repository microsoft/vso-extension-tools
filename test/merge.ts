/// <reference path="../typings/tsd.test.d.ts" />

import assert = require("assert");
import package = require("../src/scripts/package");

describe("Merger", () => {
	describe("#gatherManifests", () => {
		it("should return a string[] of filenames", () => {
			var merger = new package.Package.Merger()
		});
	});
});